import { Configuration, BlockchainDataModelEntity } from 'ew-utils-general-lib';
export interface UserProperties {
    id: string;
    organization: string;
    roles: number;
    active?: boolean;
}
export interface UserPropertiesOffChain {
    firstName: string;
    surname: string;
    street: string;
    number: string;
    zip: string;
    city: string;
    country: string;
    state: string;
}
export declare class User extends BlockchainDataModelEntity.Entity implements UserProperties {
    id: string;
    static CREATE_USER(userProperties: UserProperties, userPropertiesOffChain: UserPropertiesOffChain, configuration: Configuration.Entity): Promise<User>;
    organization: string;
    roles: number;
    active: boolean;
    configuration: Configuration.Entity;
    constructor(accountAddress: string, configuration: Configuration.Entity);
    getUrl(): string;
    sync(): Promise<User>;
}
